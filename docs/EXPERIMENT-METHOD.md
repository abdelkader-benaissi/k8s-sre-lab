# Experiment method

For every failure drill, capture: hypothesis, steady-state SLI, exact injection command, start/end timestamps, observed signals, alert behavior, mitigation, recovery proof, and corrective action. Run only one variable at a time. Stop if the blast radius leaves the `sre-lab` namespace or the stated recovery command fails.
